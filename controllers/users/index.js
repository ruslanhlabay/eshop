const signUpController = require('./sign-up.controller')
const signInController = require('./sign-in.controller')
const getAllController = require('./get-all-users.controller')

module.exports = {
    signUpController,
    signInController,
    getAllController,
}